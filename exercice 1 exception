'''
Il divise x par y, si x est plus petit que y alors 0 sinon afficher le resultat
'''

'''
'Question 2 :'
def divEntier(x: int, y: int) -> int:
    if x < y:
        return 0
    else:
        x = x - y
        return divEntier(x, y) + 1

resultat = divEntier(1, 3)
print("Résultat de la division : ", resultat)
'''


'On gere lexception ValueError car utilisateur peut rentrer tout ce quil veut'
'et parfois pas des valeurs mais des aaa par exemple'
'Quand on rentre 0 pour y, il y a une erreur car on peut pas divisé par 0 '
'Exercice :'
def divEntier(x: int, y: int) -> int:
    if y == 0:
        raise ValueError("Division par zéro n'est pas autorisée.")
    if x < 0 or y < 0:
        raise ValueError("Les nombres doivent être positifs.")
    if x < y:
        return 0
    else:
        x = x - y
        return divEntier(x, y) + 1

if __name__ == "__main__":
    try:
        x = int(input(" x : "))
        y = int(input(" y : "))

        resultat = divEntier(x, y)
        print("Résultat de la division : ", resultat)
    except ValueError as e:
        print(e)
    except Exception:
        print("Une erreur inattendue s'est produite.")
